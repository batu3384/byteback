# Phase 6 — Ops

## Delivered

- [x] NSRL MD5 lookup — `forensic::NsrlLookup`, NAPI `loadNsrl` / `lookupNsrl` / `getNsrlStats`
- [x] Case metadata — SQLite `case_info` singleton, NAPI `getCaseInfo` / `setCaseInfo`, E01 header wiring
- [x] E01 CI cross-check — optional `ewfinfo` gate (`BYTEBACK_EWFINFO`, `Ewf.OptionalEwfinfoCrossCheck`)
- [x] Fuzz harness — `test_fuzz_runs_codec.cpp` (seeded `deserializeRuns` mutations)
- [x] Tests: `test_nsrl.cpp`, `test_case_info.cpp` (+5)

## Conscious limits

- NSRL: in-memory set from user-provided file; no bundled RDS, no on-disk index for 100M+ rows
- Case mgmt: singleton row only (no multi-case archive UI)
- Fuzzing: property test on runs codec, not libFuzzer/AFL
- ewfinfo: CI skips when libewf tools absent

## Roadmap complete

Phases 0–6 delivered per `docs/superpowers/specs/2026-08-19-byteback-professional-roadmap-design.md`.
