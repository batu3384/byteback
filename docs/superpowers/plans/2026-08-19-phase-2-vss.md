# Phase 2 — VSS Mount & Snapshot Scan

**Goal:** Mount each Windows VSS shadow copy and run NTFS/FAT quick scan with `source=vss_ntfs` / `vss_fat`.

## Delivered

- [x] `DiskReader::openVolumePath()` — shadow copy device paths
- [x] `scanVssVolumeFilesystem()` — probe + NTFS/FAT scan with VSS path prefix
- [x] `scanVssSnapshots()` — enumerate, emit `vss_snapshot` marker, scan each copy
- [x] `VssSnapshotInfo.sizeBytes` from IOCTL during enumeration
- [x] `test_vss_scan.cpp` — memory NTFS volume tagged `vss_ntfs`

## Conscious limits

- Requires admin + existing VSS snapshots on live Windows host
- No VSS creation API (read-only enumeration)
- Deep carve on VSS not enabled (quick metadata only)

## Next (Phase 3)

- RAID → scan/recover wiring
- Batch recovery export
