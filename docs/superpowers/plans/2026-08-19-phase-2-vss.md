# Phase 2 â€” VSS Mount & Snapshot Scan

**Goal:** Mount each Windows VSS shadow copy and run NTFS/FAT quick scan with `source=vss_ntfs` / `vss_fat`.

## Delivered

- [x] `DiskReader::openVolumePath()` â€” shadow copy device paths
- [x] `scanVssVolumeFilesystem()` â€” probe + NTFS/FAT scan with VSS path prefix
- [x] `scanVssSnapshots()` â€” enumerate, emit `vss_snapshot` marker, scan each copy
- [x] `VssSnapshotInfo.sizeBytes` from IOCTL during enumeration
- [x] `test_vss_scan.cpp` â€” memory NTFS volume tagged `vss_ntfs`

## Conscious limits

- Requires admin + existing VSS snapshots on live Windows host
- No VSS creation API (read-only enumeration)
- Deep carve on VSS not enabled (quick metadata only)

## Next (Phase 3)

- RAID â†’ scan/recover wiring
- Batch recovery export
