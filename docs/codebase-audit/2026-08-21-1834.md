# Kod tabanı denetimi — byteback (kapsamlı UI / sistem / algoritma)

- Tarih: 2026-08-21
- Kapsam: `C:\Users\Batuhan\Desktop\byteback` (repo kökü)
- Runtime: static (`run.py`; `--runtime` yok)
- Sonuç: **BLOKE**
- Envanter: `file_count=329`, `todo_count=4` (vendored sqlite3.h), `walk_complete=true`, `complete_scan=false`, `stub-scan.complete_scan=false`

**Ölçüm notu:** `inventory.complete_scan=false` (`line_count_truncated=1`); `import-sample.complete_graph=false`. Tam TODO/stub yokluğu iddia edilmez. Vitest: **65/65** geçti (2026-08-21 18:34) — unit testler birçok UI/native bug'ı kapsamaz.

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Kritik | İşlev | `native/src/bridge/bridge_scan.cpp:507` |
| CA-002 | Kritik | İşlev | `native/src/bridge/bridge_scan.cpp:383` |
| CA-003 | Kritik | İşlev | `native/src/recovery/recovery_engine.cpp:234` |
| CA-004 | Kritik | İşlev | `src/renderer/components/HexEditor/HexEditor.tsx:107` |
| CA-005 | Kritik | İşlev | `src/main/ipc-handlers.ts:106` |
| CA-006 | Büyük | İşlev | `src/shared/scan-required.ts:22` |
| CA-007 | Büyük | İşlev | `src/renderer/components/ScanView/ScanView.tsx:191` |
| CA-008 | Büyük | İşlev | `src/renderer/components/ResultsView/ResultsView.tsx:312` |
| CA-009 | Büyük | İşlev | `native/src/scan_coordinator.cpp:381` |
| CA-010 | Büyük | İşlev | `native/src/recovery/validation.cpp:57` |
| CA-011 | Büyük | Mimari | `native/include/byteback_engine.h:23` |
| CA-012 | Büyük | Belgeler | `wiki/Home.md` |
| CA-013 | Küçük | Bakım | `src/renderer/components/ImagerView/ImagerView.tsx:79` |
| CA-014 | Küçük | Bakım | `src/renderer/App.tsx:150` |
| CA-015 | Küçük | Bakım | `src/main/ipc-handlers.ts:140` |
| CA-016 | Bilgi | Belgeler | `native/third_party/sqlite3.h` |

## CA-001 | Kritik | İşlev | native/src/bridge/bridge_scan.cpp:507

**Kanıt:** `insertFilesBatch` dönüş değeri kontrol edilmiyor; hemen ardından `fileBuffer.clear()`. Aynı pattern `flushScanToDb` (~L77).

**Neden:** SQLite rollback / disk dolu / lock durumunda bulunan dosyalar sessizce kaybolur; UI tarama bitti sanır, Results eksik kalır.

**Yön:** Batch fail → buffer tut, log/audit, scan status=Failed veya retry; clear yalnız success sonrası.

## CA-002 | Kritik | İşlev | native/src/bridge/bridge_scan.cpp:383

**Kanıt:** Resume `metadataComplete` false ise reddedilir (`Cannot resume during metadata phase`). Checkpoint yalnız metadata bitince yazılır (`scan_coordinator.cpp` ~395). `reclaimOrphanRunningScans` yarım scan'i Paused (4) yapar.

**Neden:** Metadata ortasında crash → banner "Devam et" ama native reddeder; kullanıcı kısmi sonuç görür, resume imkansız — mantık çelişkisi.

**Yön:** Metadata ilerlemesinde ara checkpoint veya pause'da metadata-complete=false iken banner/DB status honest (Stopped, resume yok).

## CA-003 | Kritik | İşlev | native/src/recovery/recovery_engine.cpp:234

**Kanıt:** LZNT1 decompress fail → yorum "fall through to raw recovery" → sıkıştırılmış cluster ham yazılır → `finishRecoverWrite` success.

**Neden:** Bozuk/sahte dosya "başarılı kurtarma" sayılır; adli güvenilirlik ihlali.

**Yön:** Decompress fail → `success=false` + açık hata; raw fallback opt-in flag.

## CA-004 | Kritik | İşlev | src/renderer/components/HexEditor/HexEditor.tsx:107

**Kanıt:** Alert: "Sıfır dolu ızgara gösterilmiyor." Grid `loading` false iken `data=[]` ile 32×16 sıfır bayt render eder (L166-169).

**Neden:** Okunamayan sektör sıfır gibi görünür — yanlış adli kanıt.

**Yön:** `readFailed || data.length===0` iken grid/entropy gizle.

## CA-005 | Kritik | İşlev | src/main/ipc-handlers.ts:106

**Kanıt:** `complete` handler scanId/generation kontrolü yok; her complete'te `setScanLive(false)` + `scan-complete` IPC.

**Neden:** Eski scan'in gecikmiş complete'i yeni scan sırasında gelirse pencere kapatma koruması düşer, yanlış log/renderer state.

**Yön:** Main'de aktif `scanId`/`generation` tut; eşleşmeyen complete ignore.

## CA-006 | Büyük | İşlev | src/shared/scan-required.ts:22

**Kanıt:** `canGenerateReport(scanId)` yalnız `scanId > 0`. Duraklatılmış oturum (`status=4`, `getLatestUsableScanId`) rapor üretebilir.

**Neden:** UI "tamamlanmış tarama" der; rapor eksik tarama istatistiği taşır.

**Yön:** `SCAN_STATUS.complete` zorunlu; paused için ayrı uyarı.

## CA-007 | Büyük | İşlev | src/renderer/components/ScanView/ScanView.tsx:191

**Kanıt:** `isFinished` yalnız Tamamlandı/İptal. `Tarama Duraklatıldı` / `Tarama Başarısız` → Durdur görünür, Sonuçları Gör yok.

**Neden:** Duraklatılmış taramada ScanView çıkışı zor; sahte "hâlâ taranıyor" hissi.

**Yön:** Terminal state genişlet; paused/failed için Results/Dashboard aksiyonları.

## CA-008 | Büyük | İşlev | src/renderer/components/ResultsView/ResultsView.tsx:312

**Kanıt:** CSV export `filteredFiles` = mevcut sayfa (~500 kayıt). Ağaç modu da sayfalı `dbFiles` üzerinde.

**Neden:** "Dışa aktar" tüm sonuç sanılır; sessiz veri kaybı.

**Yön:** Sayfalı export veya etiket "Bu sayfa (N kayıt)"; tree için lazy load.

## CA-009 | Büyük | İşlev | native/src/scan_coordinator.cpp:381

**Kanıt:** Deep scan: `prepareCarveRanges` boş → `runCarveScan` hemen return; `onProgress(totalSectors, totalSectors)` yine %100.

**Neden:** Carve hiç yapılmadan scan Complete; progress yalan.

**Yön:** Boş unalloc → status/note honest veya carve skip flag UI'da.

## CA-010 | Büyük | İşlev | native/src/recovery/validation.cpp:57

**Kanıt:** `validationScore < 60` → `validationError` set; `success` değişmez. `countsAsRecovered` success'e bakar.

**Neden:** Yapısal olarak bozuk carved dosya "kurtarıldı" sayılır.

**Yön:** Validation fail → success=false veya recover sayacından hariç tut.

## CA-011 | Büyük | Mimari | native/include/byteback_engine.h:23

**Kanıt:** Tek `DiskReader`; scan + hex + imaging eşzamanlı heavy-op guard kısmen var ama reader paylaşımlı.

**Neden:** Race ile yanlış disk offset okuma riski (düşük olasılık, yüksek etki).

**Yön:** Scan sırasında hex/imaging sıkı blok veya ayrı reader handle.

## CA-012 | Büyük | Belgeler | wiki/Home.md

**Kanıt:** `run.py docs-check`: 19 kırık relative link (`.wiki-sync`, `wiki/*.md` uzantısız hedefler).

**Neden:** Repo içi dokümantasyon navigasyonu kırık.

**Yön:** `.md` suffix veya link rewrite.

## CA-013 | Küçük | Bakım | src/renderer/components/ImagerView/ImagerView.tsx:79

**Kanıt:** `setImaging(true)` API kontrolünden önce; `startImaging` yoksa UI "İmaj Alınıyor" modunda kalır.

**Neden:** Sahte busy state (web preview / bozuk native).

**Yön:** API yoksa imaging başlatma, hata göster.

## CA-014 | Küçük | Bakım | src/renderer/App.tsx:150

**Kanıt:** Yeni scan `total: 100` placeholder; ilk IPC gelene kadar sahte yüzde.

**Neden:** Büyük diskte % sıçraması; Header pill yanıltıcı.

**Yön:** Indeterminate progress veya total=0 → "hesaplanıyor".

## CA-015 | Küçük | Bakım | src/main/ipc-handlers.ts:140

**Kanıt:** `stop-scan` hemen `setScanLive(false)`; native flush async. Catch sadece log.

**Neden:** Session log "durduruldu" derken scan devam; stop fail sessiz.

**Yön:** Complete event'e kadar scanLive veya handle + ack.

## CA-016 | Bilgi | Belgeler | native/third_party/sqlite3.h

**Kanıt:** `inventory.todo_count=4` vendored sqlite3.h içinde.

**Neden:** Upstream TODO; ürün bug değil.

**Yön:** Takip gerekmez.

## Önerilen sıra

1. CA-001 — Batch insert fail'de veri kaybını durdur.
2. CA-002 — Metadata pause/resume veya banner ile DB status hizala.
3. CA-004 — HexEditor sahte sıfır grid kaldır.
4. CA-005 — Stale scan complete race düzelt.
5. CA-003 — LZNT1 fail'de sahte success kaldır.

## Drift

Önceki: `2026-08-21.json`  
Yeni: 16 · Gitti: 9 · Aynı: 0 (fingerprint bazlı; CA-NNN her koşuda sıfırlanır)

Eklenen (özet): bridge_scan insert/resume, recovery_engine LZNT1, HexEditor, ipc-handlers scanLive, scan-required report guard, ScanView terminal UI, ResultsView export, scan_coordinator progress.

Giden (önceki rapordan): `.gitignore` sqlite (CA-001 eski — artık repoda vendor var), build.yml CI fail iddiası ayrı doğrulanmalı.
