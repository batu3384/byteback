# Codebase audit — disk

- Date: 2026-08-19
- Scope: C:\Users\Batuhan\Desktop\disk
- Runtime: static
- Result: ISSUES FOUND
- Inventory: file_count=219, todo_count=4 (run.py inventory)
- Measurement note: `complete_scan: false` (inventory `line_count_truncated=1`, `todo_skipped_large=1`; stub-scan `skipped_large=1`). No full TODO scan of sqlite3.h. No CLEAN claim.

## Summary

| Code | Level | Topic | Path |
|-----|--------|------|-----|
| CA-001 | Major | Functionality | src/preload/index.ts:82 |
| CA-002 | Major | Functionality | src/renderer/components/SmartView/SmartView.tsx:87 |
| CA-003 | Major | Functionality | native/src/fs/hfs_catalog.cpp:206 |
| CA-004 | Major | Documentation | README.md:83 |
| CA-005 | Major | Maintenance | native/src/db/metadata_store.cpp |
| CA-006 | Minor | Documentation | src/renderer/components/Layout/Sidebar.tsx:18 |
| CA-007 | Minor | Documentation | src/renderer/components/Layout/Sidebar.tsx:49 |
| CA-008 | Minor | Documentation | native/include/imager/ewf_writer.h:28 |
| CA-009 | Minor | Functionality | package.json:15 |
| CA-010 | Minor | Architecture | src/shared/ipc-contract.ts |
| CA-011 | Minor | Maintenance | src/renderer/main.tsx |
| CA-012 | Minor | Security architecture | src/renderer/components/ImagerView/ImagerView.css:56 |
| CA-013 | Info | Documentation | native/third_party/sqlite3.h |
| CA-014 | Info | Architecture | native/include/forensic/audit_logger.h:17 |
| CA-015 | Info | Execution | package.json:13 |
| CA-016 | Info | Documentation | docs/superpowers/specs/2026-08-19-byteback-professional-roadmap-design.md |
| CA-017 | Info | Functionality | native/include/fs/apfs_container.h:11 |
| CA-018 | Info | Functionality | native/include/forensic/nsrl_lookup.h:9 |
| CA-019 | Info | Functionality | native/include/imager/ewf_writer.h:23 |
| CA-020 | Info | Functionality | native/src/search/content_search.cpp:22 |

## CA-001 | Major | Functionality | src/preload/index.ts:82

**Evidence:** Native `getCaseInfo` / `setCaseInfo` / `loadNsrl` NAPI (`napi_bridge.cpp`) and `ipcMain.handle('get-case-info'|'set-case-info'|'load-nsrl'|…)` (`ipc-handlers.ts`) exist. `preload/index.ts` whitelist for `window.api` omits these calls; `types.ts` `Window.api` same. `App.tsx` / `Sidebar.tsx` define no case or NSRL page. Phase 6 plan says case mgmt + NSRL delivered. import-sample: `sample: 49 edges, 47 files`.
**Why:** Examiner cannot write case number to E01, load NSRL set, or carry case in report. Engine exists, product surface missing. Forensic workflow half-done.
**Direction:** Preload + `Window.api` + Case/NSRL screen; image and report read these fields. File selection via `dialog`, not free path.

## CA-002 | Major | Functionality | src/renderer/components/SmartView/SmartView.tsx:87

**Evidence:** ATA score `smart_monitor.cpp:277` `CA-003: UNCALIBRATED` (`eta=500`, `beta=1.5`). README labels model UNCALIBRATED. UI header: "Drive Healthy" / "Critical: Failure Risk"; `healthScore` large type, no calibration notice.
**Why:** Honest label in code and README; absent on user face. False confidence, forensic/triage error.
**Direction:** Visible "UNCALIBRATED / heuristic" text on ATA path. Keep NVMe spec fields separate. Do not label score "life expectancy".

## CA-003 | Major | Functionality | native/src/fs/hfs_catalog.cpp:206

**Evidence:** `kMaxFiles = 25000`; when `fileCount >= kMaxFiles` returns false and cuts walk. No audit event. README does not mention HFS 25k cap (`hfs_catalog.h:13` ponytail).
**Why:** Scan appears "done"; file 25,001 silently dropped. On Mac volume missing catalog is silent data loss for court/recovery.
**Direction:** Scan status/audit + UI warning when limit exceeded. Do not mark catalog walk complete early.

## CA-004 | Major | Documentation | README.md:83

**Evidence:** README says "GoogleTest (137 tests)" for `npm run test:native`; build tree `gtest_discover` 145 tests (`Ewf.OptionalEwfinfoCrossCheck` may skip). Line 53: "137 unit tests". Line 92: "Phase 0-7 (core scope) ✅"; spec phases 0–6. Line 95: both audit rounds "all resolved"; this run produces Major findings.
**Why:** Setup/test command and roadmap do not reflect reality. Operator trusts wrong count and closed audit.
**Direction:** Tie test count to `ctest` output. Phase table 0–6. Do not lock audit item as "resolved".

## CA-005 | Major | Maintenance | native/src/db/metadata_store.cpp

**Evidence:** inventory `top_by_lines`: `metadata_store.cpp` 899 lines (`package=byteback`). Exceeds 800 threshold. File: FTS5, content FTS, scans, files, timeline, case_info, search. First 80 lines FTS trigger; last 40 `setCaseInfo`.
**Why:** Single TU holds schema, search, and case. Reviewer cannot hold it; FTS trigger regression (WAL + UPDATE) hides in same file.
**Direction:** Split `case_info` and `content_fts` into separate translation units; clear `createTables` / search / case API boundaries.

## CA-006 | Minor | Documentation | src/renderer/components/Layout/Sidebar.tsx:18

**Evidence:** Menu text "Imaging (DD)". `startImaging` format `raw | ewf`; ImagerView has E01 option.
**Why:** E01 path hidden; user assumes raw only.
**Direction:** Label: "Image (RAW / E01)".

## CA-007 | Minor | Documentation | src/renderer/components/Layout/Sidebar.tsx:49

**Evidence:** Footer "v1.0.0"; `package.json` `"version": "0.1.0"`. Logo emoji (emoji not structural icon). ui-ux-pro-max: emoji is not structural icon.
**Why:** Version lie + font-dependent logo.
**Direction:** Show `package.json` version. Logo SVG (`resources/icon.svg`).

## CA-008 | Minor | Documentation | native/include/imager/ewf_writer.h:28

**Evidence:** Comment: not verified with container-independent reader. `test_ewf.cpp` `Ewf.OptionalEwfinfoCrossCheck` + CI `BYTEBACK_EWFINFO` step exist; skip if tool missing.
**Why:** Old CA-004 text falsifies Phase 6; still reads "none at all".
**Direction:** Update to "optional ewfinfo; skip if absent". Keep 4 GiB segment cap separate.

## CA-009 | Minor | Functionality | package.json:15

**Evidence:** `"test:native": "cmake --build … && ctest --test-dir native/build --output-on-failure"` — no `-C Release`. CI uses `ctest … -C Release`. Windows multi-config may pick wrong default config.
**Why:** README primary native test command runs empty/wrong config locally on Windows.
**Direction:** Add `-C Release` to script; keep `BYTEBACK_BUILD_TESTS=ON` note.

## CA-010 | Minor | Architecture | src/shared/ipc-contract.ts

**Evidence:** import-sample hubs: `ipc-contract.ts` in_edges=7; `types.ts` in_edges=4. `sample: 49 edges, 47 files`. `complete_graph: false`.
**Why:** Entire renderer contract single hub. Case/NSRL addition drifts types.ts / preload / ipc triple (root of CA-001).
**Direction:** New IPC in order contract → preload → types; single source.

## CA-011 | Minor | Maintenance | src/renderer/main.tsx

**Evidence:** import-sample orphans (js relative): `electron.vite.config.ts`, `out/renderer/assets/index-D5A_DZv5.js`, `src/renderer/main.tsx`, `vitest.config.ts`. `orphans_complete: true`. `main.tsx` Vite entry; `out/` build bundle.
**Why:** Graph partial (relative js only). `out/` sits in source tree, pollutes inventory.
**Why (continued):** Keeping production JS at repo root creates "live source" illusion.
**Direction:** `out/` gitignore; read orphan list as graph boundary, do not call "dead main.tsx".

## CA-012 | Minor | Security architecture | src/renderer/components/ImagerView/ImagerView.css:56

**Evidence:** `outline: none` — ImagerView, HexEditor, ShredderView, RaidBuilder, KeywordSearch CSS. ui-ux-pro-max: focus ring removal without replacement loses keyboard user. App runs `requireAdministrator`.
**Why:** Hiding focus in admin Electron UI increases wrong control / wrong wipe target risk.
**Direction:** Remove `outline: none` or add `:focus-visible` 2px ring (3:1).

## CA-013 | Info | Documentation | native/third_party/sqlite3.h

**Evidence:** inventory `todo_count=4`, `todo_by_file` only `sqlite3.h`; `complete_todo_list: false`, `todo_skipped_large=1`. stub-scan `hit_count=0`, `complete_scan: false`, `skipped_large=1`.
**Why:** Vendored sqlite TODOs not product debt. Full stub absence cannot be claimed.
**Direction:** Drop third_party from TODO inventory. Do not count as product TODO.

## CA-014 | Info | Architecture | native/include/forensic/audit_logger.h:17

**Evidence:** `AuditLogger::GetInstance()` process-local singleton. Scan, image, wipe, recover, case same process + admin token.
**Why:** SPOF and privilege mix conscious desktop design; not network service.
**Direction:** Model note: wipe/image same binary. No separate service, expected.

## CA-015 | Info | Execution | package.json:13

**Evidence:** runtime-check `mode=static`, `sandbox: false`, `runtime: static-only`. Plan: `kind=npm-test`, `body=vitest run`, `class=executable`, package `byteback`. Native `gtest` not in inventory `test_kinds` (`jest:1` only `entropy.test.ts`).
**Why:** Static run did not execute Vitest. Primary language cpp; native tests invisible in inventory.
**Direction:** Vitest with `--runtime`. Native ctest separate (skill does not run `npx`/`cmake`).

## CA-016 | Info | Documentation | docs/superpowers/specs/2026-08-19-byteback-professional-roadmap-design.md

**Evidence:** Spec phases 0–6. README "Phase 0-7 ✅". Phase 6 optional deliverables; no UI surface (CA-001).
**Why:** Roadmap "done" closure skips examiner surface.
**Direction:** Next spec: examiner surface (this audit's plan), not core FS depth.

## CA-017 | Info | Functionality | native/include/fs/apfs_container.h:11

**Evidence:** ponytail: linear APSB scan; no omap/file tree. README says no full APFS parser — aligned.
**Why:** Conscious ceiling, not hidden.
**Direction:** Label APFS results "volume discovery" in UI; do not imply full catalog.

## CA-018 | Info | Functionality | native/include/forensic/nsrl_lookup.h:9

**Evidence:** In-memory `unordered_set`; no full RDS. README still lists NSRL under "limits" (partly true: no embedded set) but NAPI exists (CA-001).
**Why:** Two stories: "none" vs "exists but no UI".
**Direction:** README: "MD5 set from user file; no UI" — or update when UI ships.

## CA-019 | Info | Functionality | native/include/imager/ewf_writer.h:23

**Evidence:** Single segment, table `uint32`, ~4 GiB chunk cap. No UI size warning.
**Why:** Large disk E01 silently overflows or produces broken table.
**Direction:** Before imaging reject or warn if capacity > ~4 GiB for E01 (separate phase).

## CA-020 | Info | Functionality | native/src/search/content_search.cpp:22

**Evidence:** `readFileSample` `maxBytes` (FTS first 256 KB). KeywordSearch text states this.
**Why:** Honest ceiling; not full file grep.
**Direction:** Keep. OCR/Office parse speculative.

## Recommended order

1. CA-001 — Wire Case/NSRL through preload and one screen; E01/report read fields.
2. CA-002 — Label SMART ATA score UNCALIBRATED.
3. CA-003 — Audit + UI warning at HFS 25k cutoff.
4. CA-004 — Fix README test count, phases, and audit closure.
5. CA-005 — Split `metadata_store.cpp` case/content FTS (review boundary).

## Drift

Previous: 2026-08-18.json
New: 18 · Gone: 26 · Same: 1

New (examples): Major Functionality `preload/index.ts`; Major Functionality `SmartView.tsx`; Major Functionality `hfs_catalog.cpp`; Major Documentation `README.md`; Major Maintenance `metadata_store.cpp`.

Gone (examples): Critical Functionality `bridge_scan.cpp`; Critical Functionality `ntfs_parser.cpp`; Major Security `bridge_drives.cpp`; Major Security `recovery_engine.cpp`. (18+ gone; fingerprint change, no CA-NNN comparison.)
