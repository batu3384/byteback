# Kod tabanı denetimi — byteback

- Tarih: 2026-08-21
- Kapsam: `C:\Users\Batuhan\Desktop\byteback` (repo kökü)
- Runtime: static (`run.py`; `--runtime` yok)
- Sonuç: **BLOKE** → ikinci düzeltme turu uygulandı (2026-08-21 gece); CI push sonrası doğrulanacak
- Son güncelleme (2026-08-21): heavyOps content-search, IPC hata UX, docs sync, ResultsView parçalama — **CONCERNS** (pilot OK)
- Envanter: `file_count=300`, `todo_count=4` (vendored sqlite3.h), `complete_scan=false`, `walk_complete=true`

**Ölçüm notu:** `inventory.complete_scan=false` (`line_count_truncated=1`, `todo_skipped_large=1`); `stub-scan.complete_scan=false` (`skipped_large=1`). Tam TODO/stub kapsamı iddia edilmez.

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Kritik | İşlev | `.gitignore:8` |
| CA-002 | Kritik | İşlev | `.github/workflows/build.yml` |
| CA-003 | Büyük | Mimari | `native/include/byteback_engine.h:23` |
| CA-004 | Büyük | Bakım | `src/renderer/components/ResultsView/ResultsView.tsx` |
| CA-005 | Büyük | İşlev | `src/main/ipc-handlers.ts:19` |
| CA-006 | Büyük | Belgeler | `wiki/Home.md` |
| CA-007 | Küçük | Bakım | `src/renderer/App.tsx:132` |
| CA-008 | Küçük | Bakım | `e2e/electron-smoke.spec.ts` |
| CA-009 | Bilgi | Belgeler | `native/third_party/sqlite3.h` |

## CA-001 | Kritik | İşlev | `.gitignore:8`

**Kanıt:** `.gitignore` satır 8–9 `native/third_party/sqlite3.c` ve `sqlite3.h` ignore ediyor. `git ls-files native/third_party/sqlite3.c` boş. GitHub Actions run `32426409404`: `Cannot find source file: third_party/sqlite3.c`.

**Neden:** Temiz clone ve CI native build yapamaz. README “Quick start” clone→build iddiası CI’da çöker.

**Yön:** SQLite amalgamation repoda (`native/third_party/sqlite3.c`); `.gitignore` satırları kaldırıldı.

**Durum (fix pass 2):** ✅ Uygulandı — vendor dosyalar commit edilecek.

## CA-002 | Kritik | İşlev | `.github/workflows/build.yml`

**Kanıt:** `gh run list` — son iki run FAIL (`32426409404`, `32409522380`). En yeni run Configure CMake aşamasında CA-001 yüzünden duruyor.

**Neden:** Public repo profesyonel güven için yeşil CI beklenir; şu an kırmızı.

**Yön:** CA-001 çöz → push → Windows job (native + vitest + playwright) yeşile kadar iterasyon.

## CA-003 | Büyük | Mimari | `native/include/byteback_engine.h:23`

**Kanıt:** Tek `DiskReader diskReader_` instance; audit AR20-004. `disk_reader_win.cpp` ioMutex var ama scan + hex + imaging eşzamanlı kaynak değişimi riski dokümante.

**Neden:** Forensic araçta eşzamanlı I/O race veri bütünlüğünü bozabilir.

**Yön:** Scan sırasında hex/imaging blokla veya reader handle ayır; heavyOps ile sıkılaştır.

## CA-004 | Büyük | Bakım | `src/renderer/components/ResultsView/ResultsView.tsx`

**Kanıt:** inventory `top_by_lines`: 619 satır. Tree render, recover, preview, CSV aynı dosyada.

**Neden:** Regresyon ve examiner UX değişikliği maliyeti yüksek; renderer unit test yok.

**Yön:** Recover/preview/tree parçala; en az bir davranış testi (recover guard).

## CA-005 | Büyük | İşlev | `src/main/ipc-handlers.ts:19`

**Kanıt:** DB init catch sadece `console.error`; uygulama devam eder.

**Neden:** Metadata/recover/report SQLite olmadan sessiz kırılır.

**Yön:** Startup’ta fatal banner veya scan/recover disable.

## CA-006 | Büyük | Belgeler | `wiki/Home.md`

**Kanıt:** `docs-check` 38 kırık relative link (`wiki/*.md`, `.wiki-sync/*.md` → `Roadmap`, `Security` vb. uzantısız).

**Neden:** Repo içi markdown önizleme kırık; wiki sync kaynağı GitHub Wiki syntax ile repo MD uyumsuz.

**Yön:** Wiki kaynağında `.md` suffix veya `wiki/README` link kuralları netleştir.

## CA-007 | Küçük | Bakım | `src/renderer/App.tsx:132`

**Kanıt:** `startScan` promise’inden bağımsız timer her zaman başlar; `window.api.startScan` yoksa timer durmaz.

**Neden:** “Tarama sürüyor” + artan süre yanıltıcı.

**Yön:** Timer yalnızca `id > 0` sonrası; API yoksa `failScan`.

## CA-008 | Küçük | Bakım | `e2e/electron-smoke.spec.ts`

**Kanıt:** inventory `orphan_tests`: e2e spec’ler kaynak eşleşmesi yok; 2 smoke test, report/nav guard yok.

**Neden:** Son UI hardening regresyonu CI’da yakalanmaz (native job zaten kırık).

**Yön:** scan-required + report guard e2e; CI yeşil olunca zorunlu gate.

## CA-009 | Bilgi | Belgeler | `native/third_party/sqlite3.h`

**Kanıt:** `todo_count=4`, `complete_todo_list=false`, `todo_skipped_large=1`.

**Neden:** Vendored header TODO’ları ürün borcu değil; ölçüm sınırı.

**Yön:** Dokümante et; sqlite3.c repoda olunca todo taraması yenile.

## Önerilen sıra

1. CA-001 — SQLite kaynaklarını clone/CI’da garanti altına al.
2. CA-002 — CI yeşil olana kadar workflow doğrula.
3. CA-005 — DB init fail’de examiner’ı blokla veya uyar.
4. CA-003 — `diskReader_` eşzamanlılık sertleştirmesi.
5. CA-004 — ResultsView parçalama + test.

## Drift

İlk sidecar bu kök için (`2026-08-21.json`); önceki audit `C:\Users\Batuhan\Desktop\disk` köküne ait — CA-NNN karşılaştırması anlamsız.
