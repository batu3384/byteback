# Adversarial + Bugbot + Security — byteback (2026-08-21 23:40)

Kapsam: main @ `a1d2ec6` (temiz ağaç, origin ile senkron). Vitest 65/65; seçili native 54/54; CI yeşil (#32523720911).

Security Review subagent: branch diff boş (main=origin) → çalıştırılamadı; Security Auditor persona + önceki audit kanıtı kullanıldı.

---

## Adversarial Review: scan lifecycle, progress math, recovery honesty

**Scope:** bridge_scan, scan_coordinator, recovery/validation, IPC/main, App/Sidebar/Results, fixtures/e2e  
**Verdict:** **CONCERNS**

### Critical Findings

*(yok — production outage / data-loss CRITICAL yok; resume progress rewind WARNING→CRITICAL aday ama UI/DB tutarsızlığı, kanıt kaybı değil)*

### Warnings

1. **Resume carve progress rewinds (Saboteur + Bugbot)** — `scan_coordinator.cpp` ~399–406  
   `metadataComplete=true` iken `metaBudget=0`. Carve progress `metaBudget + slice` = yalnız carve dilimi. DB’deki önceki `scanned_sectors` (metadata+carve) geriye düşer. `ScanTarget.resumeAtSector` bridge’de set edilir (`bridge_scan.cpp:442`) ama coordinator **hiç okumaz**.  
   **Etki:** Devam et → yüzde geriye zıplar; kullanıcı taramanın bozulduğunu sanır. MonotonicMeter yalnızca carve-local.  
   **Yön:** Resume’da `base = resumeAtSector` veya `metaBudget` frozen; `onProgress(max(resumeAt, meta+slice), total)`. Test: pause mid-carve → resume → progress asla azalmasın.

2. **`deliverScanComplete` son çare scanContext sızdırır (Saboteur + Bugbot)** — `bridge_scan.cpp:136–138`  
   İki BlockingCall fail → yalnız `endHeavyOp()`; `scanContext` kalır → `isScanActive()==true` → sleep fallback `broadcastScanComplete` atlar; main `scanLive` sıkışabilir.  
   **Yön:** Fail path’te `scanContext.reset()` + `tsfn.Release()` mümkünse; değilse main watchdog `isScanActive` false zorla.

3. **Sidebar `hasValidScanId` vs rapor `isCompleteScan` (New Hire + Security)** — `Sidebar.tsx` / `scan-required.ts`  
   Sonuçlar/arama paused (status=4) ile açılır; adli rapor yalnız complete. Bilinçli ama yeni geliştirici “scan-ready” tek anlam sanır.  
   **Yön:** `scanReady` = usable; `reportReady` = complete — isimleri ayır veya yorum.

### Notes

4. **`resumeAtSector` ölü alan (New Hire)** — ScanTarget’ta yazılıyor, deep/full_carve path’te tüketilmiyor. Ya kullan ya sil.

5. **Tek shared DiskReader (Saboteur fragile assumption)** — heavyOps ile seri; paralel hex+scan yok. Mimari tavan belgelenmiş; kabul edilebilir.

6. **E2E nav-* testids (New Hire)** — Label kırılganlığı giderildi; iyi. CI Node 20 deprecation uyarısı fail değil.

7. **PNG fixture artık yapısal geçerli (Saboteur)** — CA-010 honesty ile uyumlu; golden yeşil.

### Summary

Tarama/kurtarma dürüstlüğü büyük ölçüde toparlandı; CI yeşil. En önemli açık: **resume sonrası progress matematiği** (`metaBudget=0` + kullanılmayan `resumeAtSector`) — kullanıcıya yanlış yüzde. İkinci: nadir TSFN total failure’da stuck scanLive.

---

## Bugbot

| Severity | Location | Finding |
|----------|----------|---------|
| High | `scan_coordinator.cpp:399-404` | Resume carve progress rewinds (metaBudget=0; resumeAtSector unused) |
| Medium | `bridge_scan.cpp:136-138` | Complete delivery failure leaves scanContext / IsScanActive stuck |

---

## Security Review

Subagent: **empty diff** (main = origin) — otomatik diff incelemesi yok.

Manuel Security Auditor (önceki audit + kod):

| Severity | Location | Finding |
|----------|----------|---------|
| Medium (bilinen, kısmen kapatıldı) | ResetScanDatabase | `SCAN_DB_RESET` audit eklendi (NF-005); session.log hâlâ zincirsiz |
| — | hex / imaging / recover paths | Limit + allowlist + destDir güvenliği önceki turda güçlendirildi; yeni kritik exploit yolu görülmedi |
| Note | Electron tek kullanıcı | Cross-tenant yok; yerel examiner threat model |

**Security özeti:** Diff boş olduğu için yeni otomatik bulgu yok. Bilinen yüzeyler (IPC token, hex 64KiB, imaging allowlist, LZNT1/validation honesty) mevcut main’de korunuyor. Kritik açık: yukarıdaki resume progress (integrity/UX, remote RCE değil).

---

## Test kanıtı

| Suite | Sonuç |
|-------|--------|
| Vitest | 65/65 |
| Native (Golden/Metadata/Recovery/ScanCoordinator filter) | 54/54 |
| CI #32523720911 | success |

---

## Önerilen sonraki iş (öncelik)

1. Resume progress: `resumeAtSector` / frozen base — **DÜZELTİLDİ** (`metaShare` + `MonotonicMeter`)
2. `deliverScanComplete` fail → scanContext teardown — **DÜZELTİLDİ**
3. Sidebar ready semantics — **DÜZELTİLDİ** (report=complete, results=usable id)

## Fix pass (2026-08-21 23:48)

- `runDeepScan` / `runFullCarveScan`: `carveProgressBase = metaShare` when metadataComplete; overall `MonotonicMeter` seeded from `resumeAtSector`
- `deliverScanComplete`: last-resort `tsfn.Release` + `scanContext.reset` + `endHeavyOp`
- Sidebar: `reportReady` via `canGenerateReport(scanState)`; results via `hasValidScanId`
- Test: `ScanCoordinator.DeepResumeProgressNeverRewinds`
