# Third-party sources (vendored)

## SQLite

- `sqlite3.c` / `sqlite3.h` — official SQLite amalgamation (3.45.x), committed for reproducible CI and clone builds.
- Do not delete; CMake links this into `byteback_engine`.
